/**
 * Компонент отвечает за отрисовку виджета Посещяемое
 * @param {string} title
 * @param {Array.<Object>} visitedList
 */

function VisitedWidget(props) {
  const {title, visitedList} = props;
  return ();
}

export default VisitedWidget;
