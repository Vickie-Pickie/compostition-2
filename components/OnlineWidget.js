/**
 * Компонент отвечает за отрисовку виджета Эфир
 * @param {string} title
 * @param {Array.<Object>} onlineList
 */

function OnlineWidget(props) {
  const {title, onlineList} = props;
  return ();
}

export default OnlineWidget;
