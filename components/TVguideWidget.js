/**
 * Компонент отвечает за отрисовку виджета Телепрограмма
 * @param {string} title
 * @param {Array.<Object>} tvProgramList
 */

function TVguideWidget(props) {
  const {title, tvProgramList} = props;
  return ();
}

export default TVguideWidget;
