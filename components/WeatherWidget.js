/**
 * Компонент отвечает за отрисовку виджета Погода
 * @param {string} icon
 * @param {string} temp
 * @param {string} tempDay
 * @param {string} tempMorning
 */

function WeatherWidget(props) {
  const {icon, temp, tempDay, tempMorning} = props;
  return ();
}

export default WeatherWidget;
