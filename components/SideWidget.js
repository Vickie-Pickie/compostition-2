/**
 * Компонент отвечает за отрисовку бокового виджета
 * @param {string} icon
 * @param {string} title
 * @param {string} text
 */

function SideWidget (props) {
  const {icon, title, text} = props;
  return ();
}

export default SideWidget;
